import { Box, styled } from "@mui/material";

/**
 * @description Styled header
 *
 * @area : Login Page, Forgot Password Page
 */
export const StyledHeaderBox = styled(Box)(({ theme }) => ({
  width: "fit-content",
  margin: "auto",
}));
